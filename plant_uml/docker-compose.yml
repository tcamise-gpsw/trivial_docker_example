services:
    plant-uml-cli:
        build:
            context: .
        container_name: plant-uml-cli
        volumes:
            - ./shared_volume:/shared_volume
        working_dir: /shared_volume
        # entrypoint: 'bash' # For debugging
